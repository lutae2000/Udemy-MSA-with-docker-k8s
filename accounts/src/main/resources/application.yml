server:
  port: 8080
spring:
  application:
    name: "accounts"
  profiles:
    active: "prod"
  datasource:
    url: jdbc:mariadb://192.168.31.7:33066/webapp?characterEncoding=UTF-8
    username: utlee
    password: p@ssword1
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    show-sql: true
  config:
    import: "optional:configserver:http://localhost:8071/"
  sql:
    init:
      mode: always
#  rabbitmq:
#    host: "localhost"
#    port: 5672
#    username: "guest"
#    password: "guest"

management:
  endpoints:
    web:
      exposure:
        include: "*"